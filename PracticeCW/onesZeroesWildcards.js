// make array [1, 1, X, X, 0, X]
// get loc #s of wildcards [2, 3, 5]

// use loc array length to generate perms base2
// (also need to front fill with zeros)
// for (var i = 1; i <= 16; i++) {
//      push to an fillArray [001, 010 etc.]
//    console.log(i.toString(2));
//}

// loop through fillArray, creating a new array that looks like answer